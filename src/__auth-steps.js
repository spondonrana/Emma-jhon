/**
 * 1. create a new project in console.google.com
 * 2. npm install firebase
 * 3. create firebase.init.js and import getAuth to export auth
 * 4. firebase setting > Authentication > enable email & password auth
 * 5. create login signup components, set route,
 *  6. attatch form field handler and from submit handler
 *  7. npm install --save react-firebase-hooks
 *  8. useCreateUserWithEmailAndPassword from react-firebase-hooks
 *  9. if user is created redirect to shop page
 *  10. useSignInWithEmailAndPassword(auth) for sign in
 *
 */

/**
 * Hosting Steps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 *
 *
 */
